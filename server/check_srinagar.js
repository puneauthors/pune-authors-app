const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const e = await prisma.event.findFirst({ where: { name: { contains: 'SRINAGAR' } } }); console.log(e); if (!e) { const all = await prisma.event.findMany({ select: { id: true, name: true, broadcastStatus: true } }); all.filter(x => x.name.toLowerCase().includes('srinagar')).forEach(x => console.log(x)); } } run().finally(() => prisma.$disconnect());
