const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const event = await prisma.event.findFirst({ where: { name: 'Shillong Book Fair' } }); await prisma.posOrderItem.deleteMany({ where: { posOrder: { eventId: event.id } } }); await prisma.posOrder.deleteMany({ where: { eventId: event.id } }); console.log('Deleted POS orders for event ' + event.id); } run().finally(() => prisma.$disconnect());
